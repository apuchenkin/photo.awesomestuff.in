User
    ident Text
    password Text Maybe
    UniqueUser ident
    deriving Typeable

--Email
--    email Text
--    user UserId Maybe
--    verkey Text Maybe
--    UniqueEmail email

 -- By default this file is used in Model.hs (which is imported by Foundation.hs)

Category
    name Text
    deriving Show

Photo json
    name String
    src String
    thumb String Maybe
    width Int
    height Int
    views Int
    category CategoryId Maybe sqltype=int -- sqltype is workaround due to bug in persistent (uses bigint instead)
    deriving Show
